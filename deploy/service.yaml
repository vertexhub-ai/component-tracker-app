apiVersion: v1
kind: Service
metadata:
  name: component-tracker-app
  namespace: component-tracker-app
spec:
  selector:
    app: component-tracker-app            # GOTCHA: matches Deployment pod label exactly
  ports:
    - name: http
      port: 80
      targetPort: 3000
