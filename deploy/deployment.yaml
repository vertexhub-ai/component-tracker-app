apiVersion: apps/v1
kind: Deployment
metadata:
  name: component-tracker-app
  namespace: component-tracker-app
  labels:
    app: component-tracker-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: component-tracker-app
  template:
    metadata:
      labels:
        app: component-tracker-app          # GOTCHA: Service selector MUST match this exactly, or endpoints stay empty (503)
    spec:
      containers:
        - name: component-tracker-app
          image: localhost:30500/component-tracker-app/api:05ca244
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: HOST
              value: "0.0.0.0"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: component-tracker-app-secrets
                  key: database-url
          livenessProbe:
            httpGet:
              path: /api/health   # GOTCHA: must match the app's real health route
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
